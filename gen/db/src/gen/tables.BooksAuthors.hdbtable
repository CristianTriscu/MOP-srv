-- generated by cds-compiler version 3.5.4
COLUMN TABLE tables_BooksAuthors (
  author_id INTEGER NOT NULL,
  book_id INTEGER NOT NULL,
  PRIMARY KEY(author_id, book_id)
) WITH ASSOCIATIONS (
  MANY TO ONE JOIN tables_Authors AS author ON (author.id = author_id),
  MANY TO ONE JOIN tables_Books AS book ON (book.id = book_id)
)